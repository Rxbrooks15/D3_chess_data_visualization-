d3.csv("raine_chess_data.csv").then(function(data) {
    // Get the column names
    var columnNames = Object.keys(data[0]);
  
    console.log(columnNames);
  });

  d3.csv("raine_chess_data.csv").then(function(data) {
    console.log(data);
  });

  //Checking data and viewing dataset